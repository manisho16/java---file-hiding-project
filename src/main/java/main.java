import views.welcomepage;
public class main {
    public static void main(String[] args){
    welcomepage w=new welcomepage();
    do{
        w.welcomeScreen();
    }while(true);
    }
    //the code id fully ready
    //the only issue you would face with it is that it cant hide folders right now
    //as the system dhows access denied if a folder is choosen
}
